/* Middlewares/Third_Party/ports/rtconfig.h */
#ifndef _RTCONFIG_H_
#define _RTCONFIG_H_

/* 
 * 这是一个在 FreeRTOS / 裸机环境下为 FAL 提供的桩 (Stub) 头文件。
 * 目的：满足 FAL 源码中 `#include <rtconfig.h>` 的编译依赖，
 * 避免直接修改 FAL Git 子模块的源码，确保第三方库保持干净。
 */

#endif /* _RTCONFIG_H_ */